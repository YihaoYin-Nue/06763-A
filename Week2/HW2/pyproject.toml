[project]
name = "a02-sensors"
version = "0.1.0"
description = "06763 A2 - Intel Lab sensor data into SQLite + DuckDB"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.1",
    "pandas>=2.2",
    "pyarrow>=17",
]

# Not an installable package - just a pinned environment for scripts.
[tool.uv]
package = false
